# status 
git status

# criar Branch
git checkout -b NovaBranch

#ver as branchs
git branch

# mudar 
git checkout master

# deletar
git checkout -d NovaBranch

# Unir branchs - com merge - complexo

# Unir branchs - com rebase - complexo

# Guardar alterações que não foram commitadas em um arquivo
# pode chmar quando achar necesspario (parâmetros list, clear, drop)
git stash

# trabalhando com alias no git
git config --global alias.s status

# trabalhando com tags
git tag -a <valor> -m "mensagem"
git tag -a 1.0.0 -m "Readme finalizado"

#subir as tags
git push origin Master --tags

# reverter (reverte mudanças, código anterior) - git reset, perde tudo
git revert

# apagar repositório remoto
g tag 

# local
git tag -d <nome da tag>
git tag -d 1.0.0
